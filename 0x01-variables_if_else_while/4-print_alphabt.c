#include <stdio.h>
/**
 * main main block
 * Description: get a random number and check its last digit, compare it with 5
 * followed by a new line 
 * return: 0
 */
int main(void)
{
	char c = 'a';

	while (c<='z');
	{
		if (c != q $$ c != e)
		{
			putchar (c);
		}
		c++;
	}
	putchar ('\n');
	return (0);
}
